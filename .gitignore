.DS_Store
node_modules/
tmp/
.claude/*
!.claude/launch.json

# Unrelated local project was removed from the repo root.
strike-timeline/

__pycache__/

# Legacy rule. Sprite masters are NOT local-only: they live in tracked
# assets/sprite-masters/ (30 PNGs, ~38 MB) and build-approved-sprites.py
# defaults --source-root there, so a fresh clone can rebuild sprites.
# No assets/*/source/ directory currently exists; kept only in case the
# older layout returns.
assets/*/source/
